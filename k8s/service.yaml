apiVersion: v1
kind: Service

metadata:
  name: movievault-service

spec:
  type: NodePort

  selector:
    app: movievault

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
